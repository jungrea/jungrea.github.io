---
title: '原始大模型响应的完整结构示例response'
description: 'Agent 系统中的核心数据结构汇总：一次看懂所有数据模型。'
pubDate: 2026-12-02
tags: ['架构', '数据结构']
---

# 原始大模型响应的完整结构示例response

```
Message(
    id='0a2f29fe-eba5-49a2-b053-095aade1c075',
    container=None,
    content=[
        TextBlock(
            citations=None,
            text='我来查看test.py文件的内容。',
            type='text'
        ),
        ToolUseBlock(
            id='call_00_dmd7uCge9eTER1itzxi3HXJv',
            caller=None,
            input={
                'path': 'test.py'
            },
            name='read_file',
            type='tool_use'
        )
    ],
    model='deepseek-chat',
    role='assistant',
    stop_details=None,
    stop_reason='tool_use',
    stop_sequence=None,
    type='message',
    usage=Usage(
        cache_creation=None,
        cache_creation_input_tokens=0,
        cache_read_input_tokens=512,
        inference_geo=None,
        input_tokens=10,
        output_tokens=50,
        server_tool_use=None,
        service_tier='standard'
    )
)
```

## 结构说明

1. **消息基本信息**：
   - ID: `0a2f29fe-eba5-49a2-b053-095aade1c075`
   - 模型: `deepseek-chat`
   - 角色: `assistant`
   - 停止原因: `tool_use` (表示使用了工具)
   - 消息类型: `message`

2. **消息内容**：
   - **文本块**：表示要查看 test.py 文件的内容
   - **工具调用块**：
     - 工具名称：`read_file`
     - 工具输入：`{"path": "test.py"}`
     - 工具ID：`call_00_dmd7uCge9eTER1itzxi3HXJv`

3. **使用统计**：
   - 输入 tokens: 10
   - 输出 tokens: 50
   - 缓存读取输入 tokens: 512

4. **执行流程**：
   - 智能体决定查看 test.py 文件的内容
   - 使用 `read_file` 工具读取文件
   - 工具执行后会返回文件内容
   - 智能体将根据文件内容进行下一步操作